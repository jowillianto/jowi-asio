module;
#include <cstdint>
#include <memory>
export module jowi.asio.lockfree:ringbuf_queue;
import :shared_ptr;
import :tagged_ptr;

namespace jowi::asio {
  struct ringbuf_queue_node {
  private:
  };
}
